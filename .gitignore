# secrets
.env

# python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# runtime data. NOTE: calm_money.db IS tracked — GitHub Actions persists state
# by committing it back after each run. Only the transient journal + media are ignored.
data/calm_money.db-journal
data/generated_images/
data/covers/
data/reels/
data/*.lock
data/KILL_SWITCH

# music tracks (drop your own royalty-free files here; not committed)
assets/music/*.mp3
assets/music/*.m4a
logs/
*.log

# fonts ARE tracked (committed for CI so runs don't re-download them).

# review artifacts from the Phase 2 generation run
review/

# OS
.DS_Store
Thumbs.db
